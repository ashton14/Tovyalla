{
  "name": "tovyalla-crm",
  "version": "1.0.0",
  "description": "CRM system for pool construction company",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n server,client \"npm run server\" \"npx wait-on tcp:127.0.0.1:5000 -t 30000 && npm run client\"",
    "server": "node backend/server.js",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["crm", "pool", "construction"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^8.0.1"
  }
}

